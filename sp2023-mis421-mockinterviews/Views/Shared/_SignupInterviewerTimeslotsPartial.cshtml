@model IEnumerable<sp2023_mis421_mockinterviews.Models.MockInterviewDb.SignupInterviewerTimeslot>

<h3>Upcoming Interviewer Bookings</h3>
@if (Model.Count() > 0)
{
    <table class="table">
        <thead>
            <tr>
                <th>Date</th>
                <th>Time</th>
                <th>Location</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var signupInterviewerTimeslot in Model)
            {
                <tr>
                    <td>@signupInterviewerTimeslot.Timeslot.EventDate.Date</td>
                    <td>@signupInterviewerTimeslot.Timeslot.Time</td>
                    @if(signupInterviewerTimeslot.SignupInterviewer.InPerson)
                    {
                        <td>In Person</td>
                    }
                    else
                    {
                        <td>Virtual</td>
                    }
                    <td>
                        @Html.ActionLink("Cancel",
                            "Delete",   // <-- ActionMethod
                            "SignupInterviewerTimeslots",  // <-- Controller Name.
                            new { signupInterviewerTimeslot.Id }, // <-- Route arguments.
                            null
                            )

                    </td>   
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>No upcoming events</p>
}
